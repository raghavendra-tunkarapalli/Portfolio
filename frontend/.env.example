# Optional: Set this in Netlify / Vercel Environment Variables when your backend is deployed to cloud
# If left blank, it defaults to relative path /api/contact (proxied locally in Vite)
VITE_API_BASE_URL=
# Example for Render / Railway backend:
# VITE_API_BASE_URL=https://portfolio-backend.onrender.com
